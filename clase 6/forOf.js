/*const numbers = [1, 2, 3, 4, 5, 6, 7];

let sum = 0;

for (const number of numbers) {

    sum += number;
}

console.log(`La sumatoria es ${sum}`);*/

//Ejercicio
//CREAR UN NUEVO ARRAY Y CALCULAR LA MULTIPLICACION DE LOS ELEMENTOS DEL ARRAY
const numbers = [1, 2, 3, 4, 5, 6, 7];

let multiplicacion = 1;

for (const number of numbers) {//nombre de la variable q se va a utilizar y la segundo es el nombre del array

    multiplicacion *= number;
}

console.log(`La sumatoria es ${multiplicacion}`);